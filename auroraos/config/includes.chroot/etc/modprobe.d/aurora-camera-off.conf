# AuroraOS — camera OFF by default.
# Blacklist the USB Video Class driver so no webcam is usable until the user
# explicitly enables it (Privacy ▸ "Camera"). Nothing can grab the camera
# silently because the kernel module isn't even loaded.
blacklist uvcvideo
